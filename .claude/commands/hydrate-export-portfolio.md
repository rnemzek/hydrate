---
description: Export a structured tech-overview.json from .hydrate/ journals, package.json, and ROADMAP.md.
---

Run `hydrate export-portfolio $ARGUMENTS` in the project root using the Bash
tool (with no arguments this writes `./tech-overview.json`), then confirm the
output path and summarize the exported overview, architecture, stack, and
roadmap sections for the Product Owner.
