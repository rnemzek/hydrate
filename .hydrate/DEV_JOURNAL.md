# Developer Journal - hydrate

# hydrate — System Documentation

## 1. Living Architecture & System Overview
`@nemzilla/hydrate` is a zero-dependency Node.js CLI (`bin/cli.js`) that scaffolds and maintains a 3-artifact AI context harness inside any target repo:

- **`CLAUDE.md`** — Operating rules & AI execution protocol, rendered from `templates/CLAUDE.md.template`.
- **`.hydrate/CURRENT_UOW.md`** — The active Unit of Work execution canvas, rendered from `templates/CURRENT_UOW.md.template`.
- **`docs/SYSTEM.md`** — This file's own shape: living architecture notes, roadmap/task index, backlog, and an append-only decision log, rendered from `templates/SYSTEM.md.template`.

**Module map**
- `src/templates.js` — loads/renders `templates/*.template` files (`{{PLACEHOLDER}}` substitution).
- `src/init.js` — `scaffold()` idempotently writes the 3 canonical artifacts into a target repo.
- `bin/cli.js` — command dispatch for the strict v2 surface (`init`, `prompt`, `clip`, `complete`, `help`, plus `-h`/`-v`/`?` aliases); owns `prompt` (sync active UOW payload from `docs/SYSTEM.md` + `CLAUDE.md`) and `complete` (mark done in `docs/SYSTEM.md`, log to Section 4, archive, reset) directly.
- `src/clipboard.js`, `src/help.js` — clipboard piping and CLI help/usage text.

**Pruned in UOW-02 (v2 Refactor):** `src/adopt.js`, `src/inject.js`, `src/discover.js`, `src/guide.js`, `src/setupCc.js` — the v1 `adopt`/`inject`/`iterate`/`setup-cc`/`greenfield`/`brownfield`/`next`/`lost`/`copy` command entry points and their backing modules.

## 2. Tactical Roadmap & Task Index
- [x] **UOW-01:** Core CLI Inject & Init Engine (`.hydrate/` setup)
- [x] **UOW-02:** Guidance Engine (`hydrate ?` / `hydrate lost`)
- [x] **UOW-03:** Portfolio Featured Card Integration (`robert.nemzilla.net`)
- [x] **UOW-04:** Native System Clipboard Pipe (`hydrate clip`) & Strict Task Validation (`v1.1.0`)
- [x] **UOW-05:** Universal Brownfield Adoption Engine & Claude Code Integration (`hydrate adopt` / `hydrate setup-cc`)
- [x] **UOW-09:** Universal Git Hygiene, Brownfield Status Heuristics & Repo Fingerprinting
- [x] **UOW-01 (v2 Refactor):** Streamlined CLI Harness & 3-Artifact Scaffolding (Iterated: 0)

## 3. Backlog & Feature Parking Lot (Brainstorm)
- **UOW-06:** Scope Guardrails (`hydrate diff` / File Boundary Inspector)
- **UOW-07:** Pre/Post Execution Hook Runner (`hydrate run`)
- **UOW-08:** Telemetry & Token Accounting (`.hydrate/session.json`)

## 4. Decision & Execution Log
<!-- Append-only history of completed UOWs -->

### UOW-01 — completed 2026-08-28
- Iterations logged: 0
- Suggested commit: `feat: complete UOW-01`

### UOW-02 — completed 2026-08-28
- Iterations logged: 0
- Suggested commit: `feat: complete UOW-02`

### UOW-HYDRATE-01 — completed 2026-08-31
**Scaffold Engine & Template Migration to `.hydrate/` Journal Architecture**

- Migrated `hydrate init` off the legacy 3-artifact scaffold (`CLAUDE.md`, `.hydrate/CURRENT_UOW.md`, `docs/SYSTEM.md`) onto the 6-artifact layout: `CLAUDE.md` plus `.hydrate/{CURRENT_UOW,ROADMAP,PROJECT_JOURNAL,DEV_JOURNAL,ARCHITECT_JOURNAL}.md` and `.hydrate/archive/`.
- Added `templates/.hydrate/{CURRENT_UOW,ROADMAP,PROJECT_JOURNAL,DEV_JOURNAL,ARCHITECT_JOURNAL}.md.template`; removed the stray non-`.template`-suffixed files that `loadTemplate()` could never have resolved (`templates/{ARCHITECT_JOURNAL,DEV_JOURNAL,PROJECT_JOURNAL,ROADMAP}.md`) and the now-missing `templates/SYSTEM.md.template` dependency that was crashing `scaffold()`.
- Rewrote `src/init.js`'s `scaffold()` to render the 5 `.hydrate/` templates via a small `{name, label}` table instead of one-off blocks; still idempotent (skips any file that already exists) and returns only the files it actually created.
- Rewired `bin/cli.js`'s `prompt`/`complete` commands off `docs/SYSTEM.md` onto the new journal files: `findNextPendingUow()` now scans `.hydrate/ROADMAP.md`'s "## Section 1: Scheduled Roadmap Items" instead of `docs/SYSTEM.md`'s "## 2."; `hydrate complete` now flips the matching bullet in `.hydrate/ROADMAP.md` and appends the completion entry to `.hydrate/PROJECT_JOURNAL.md` instead of `docs/SYSTEM.md` Section 4. `docs/`/`docs/SYSTEM.md` are no longer created or referenced by bootstrap.
- Updated `src/help.js` command summaries/workflow banner and `test/{init,prompt,complete,cli}.test.js` to match — `cli.test.js`'s stale `doesNotMatch(/ROADMAP\.md/)` assertion (a leftover from pruning v1's root-level `ROADMAP.md`) was removed since `.hydrate/ROADMAP.md` is now an intentional v2 artifact.
- Verified end-to-end by hand in a scratch dir: `init` → edit `.hydrate/ROADMAP.md` → `prompt` (pulls the pending UOW, writes the fingerprinted payload) → `complete` (marks the roadmap bullet `[x]`, appends to `PROJECT_JOURNAL.md`, archives the canvas).
- Test suite: 48/48 passing. Coverage: `src/init.js` 100% line/branch; `bin/cli.js` 93.48% line / 73.81% branch; `src/help.js` 93.99% line / 72.00% branch — all new branches added by this UOW are covered; the uncovered lines are pre-existing edge cases (malformed `package.json`, `--chunk-size` parsing, non-chunked architect payload path) outside this UOW's surgical scope.

### UOW-HYDRATE-01-HOTFIX — completed 2026-09-01
**Fix UOW ID Regex Parser for Hyphenated & Alphanumeric Identifiers**

- `bin/cli.js:251` — `handleComplete()`'s UOW-ID extraction regex was `/UOW-[\d\w]+/`, which stops at the first `-` (since `\w` excludes hyphens) and truncated hyphenated IDs like `UOW-HYDRATE-01` down to `UOW-HYDRATE`, dropping the `-01` sub-slug. Confirmed as the only occurrence of a UOW-ID regex in the codebase (`grep -rn "UOW-\[" bin src`).
- Fixed by widening the character class to `/UOW-[A-Za-z0-9-]+/`, so hyphens inside the ID are retained; matching still stops naturally at the first non-alphanumeric/non-hyphen character (`:`, `*`, whitespace), so existing single-segment IDs (`UOW-42`) are unaffected.
- Added a parameterized regression test in `test/complete.test.js` covering `UOW-HYDRATE-01`, `UOW-HOTFIX-03`, and `UOW-CARBOYZ-12` end-to-end (stdout message, suggested commit, `.hydrate/archive/<id>.md` filename, `.hydrate/PROJECT_JOURNAL.md` entry), plus a dedicated test confirming the `.hydrate/ROADMAP.md` bullet-flip regex also preserves the full hyphenated ID.
- Verified by hand in a scratch dir: `init` → set `.hydrate/CURRENT_UOW.md` to `## UOW-HYDRATE-01: Fixture` → `complete` → archived to `.hydrate/archive/UOW-HYDRATE-01.md` (previously would have been `UOW-HYDRATE.md`).
- Test suite: 52/52 passing (48 prior + 4 new). No coverage regressions.

### UOW-HYDRATE-02 — completed 2026-09-01
**Implement Journal & Archive Validation Utility (`hydrate check`)**

- Added `src/commands/check.js` (`runCheck(cwd)` / `formatReport(result)`) and wired a new `hydrate check` command into `bin/cli.js`'s dispatch switch (`handleCheck()`) plus a `COMMANDS.check` entry in `src/help.js`.
- `runCheck()` reads every `<UOW-ID>.md` filename under `.hydrate/archive/` as the source of truth for archived UOW IDs, then for each ID asserts: a completed checklist line `- [x] **[<UOW-ID>]**` in `.hydrate/PROJECT_JOURNAL.md`, and a markdown heading referencing `<UOW-ID>` in `.hydrate/DEV_JOURNAL.md` and `.hydrate/ARCHITECT_JOURNAL.md`.
- **Deliberate deviation from the UOW spec's literal `## [<UOW-ID>]` heading format:** `DEV_JOURNAL.md`/`ARCHITECT_JOURNAL.md` are hand-maintained (CLAUDE.md §4) and this repo's own established convention is `### <UOW-ID> — completed <date>` (see UOW-HYDRATE-01/-01-HOTFIX entries above), not `## [<UOW-ID>]`. A literal implementation of the spec's format would fail-close against this repo's own real journals, contradicting Acceptance Criterion 1 ("a complete `.hydrate/` structure passes with exit code 0"). Used a lenient heading matcher (`hasJournalHeading()`: any `#{1,6}` line containing the UOW ID token) that accepts both styles instead.
- Also implements the spec's "warn/flag if `CURRENT_UOW.md` contains a completed UOW payload not yet archived" requirement (`findUnarchivedCompletedUow()`): fires when `CURRENT_UOW.md` resolves a UOW ID, has zero remaining `- [ ]` tasks, isn't the reset placeholder, and that ID isn't already in `.hydrate/archive/` — mirroring the `findOpenTasks()` heuristic `hydrate complete` already uses. Per Acceptance Criterion 2, this warning also flips the overall exit code to `1` (not just the missing-journal-entry errors).
- `hydrate check` exits `0` only when there are zero missing-entry errors and zero unarchived-UOW warnings; exits `1` otherwise. A missing `.hydrate/` directory is reported as a single top-level error (exit `1`); an existing `.hydrate/` with an empty/missing `archive/` reports "No archived UOWs found" and exits `0`.
- Added `test/check.test.js` (12 tests) covering: missing `.hydrate/`, empty structure (pass), fully-consistent archive (pass), each of the three journal files missing an entry independently, all three journal files absent, the unarchived-completed-UOW warning, no warning when tasks are still open / already archived / reset to placeholder, and multiple archived UOWs validated independently.
- Test suite: 64/64 passing (52 prior + 12 new). Coverage: `src/commands/check.js` 100% line / 90.48% branch / 100% funcs — clears the 80% line+branch gate; `bin/cli.js` 93.73% line / 74.42% branch (the new `handleCheck()` lines are fully covered; uncovered lines are pre-existing, out-of-scope edge cases).

### UOW-HYDRATE-03 — completed 2026-09-01
**Implement High-Density Context Compiler (`hydrate context`)**

- Added `src/commands/context.js` (`buildContext(cwd, { depth })`, plus exported helpers `extractRecentArchitectEntries()` and `extractActiveEpics()` — kept pure/testable, following `src/commands/check.js`'s split) and wired a new `hydrate context` command into `bin/cli.js`'s dispatch switch (`handleContext()`) plus a `COMMANDS.context` entry in `src/help.js`.
- `buildContext()` compiles a single string: a project/timestamp header, the full trimmed contents of `.hydrate/CURRENT_UOW.md` (or a "no active UOW" fallback notice), the tail `depth` (default 3) "### " decision-log blocks from `.hydrate/ARCHITECT_JOURNAL.md`, and the `## Section 1: Scheduled Roadmap Items` section of `.hydrate/ROADMAP.md` (falls back to the full trimmed roadmap text if that heading is absent). Each artifact independently reports its own fallback notice if missing, rather than failing the whole command.
- `handleContext()` supports `--depth <n>` and `--depth=<n>` (invalid/non-positive values silently fall back to the default of 3, matching `hydrate prompt --chunk-size`'s existing parse-and-fallback convention) and `--clip`/`-c`, reusing `copyWithFeedback()` (`printFallback: false`, since the payload is already printed to stdout before the copy attempt).
- Small deliberate duplication: `context.js` carries its own copy of `getProjectName()` rather than requiring it from `bin/cli.js`, since `bin/cli.js` executes top-level command dispatch on require and isn't safe to import as a library module.
- Added `test/context.test.js` (21 tests): unit coverage of `extractRecentArchitectEntries()` (null/empty, depth 0, tail-slicing, depth exceeding count, ignoring pre-heading content) and `extractActiveEpics()` (null input, section extraction/truncation, fallback to full text); `buildContext()` fallback notices, full compilation, and whitespace-only CURRENT_UOW.md handling; CLI-level coverage of stdout output, `--depth`/`--depth=`/invalid-depth handling, the bare-`.hydrate/` fallback path, `--clip`/`-c` piping through a fake clipboard binary (mirrors `clipboard.test.js`'s fake-bin approach), the no-clipboard-tool fallback notice (payload not reprinted), the no-`--clip` no-op path, and `--help`.
- Test suite: 85/85 passing (64 prior + 21 new). Coverage: `src/commands/context.js` 100% line / 97.30% branch / 100% funcs — clears the 80% line+branch gate; `bin/cli.js` 94.67% line / 77.78% branch (new `handleContext()` lines fully covered; uncovered lines are pre-existing, out-of-scope edge cases).

### UOW-HYDRATE-04 — completed 2026-09-01
**Implement Static Portfolio Overview Exporter (`hydrate export-portfolio`)**

- Added `src/commands/export-portfolio.js` exporting a pure `buildPortfolio(cwd)` (plus testable helpers `buildOverview()`, `buildArchitecture()`, `buildStack()`, `buildRoadmap()`, `extractCompletedUows()`, `parseRoadmapLine()`) and wired `hydrate export-portfolio` (alias `hydrate export`) into `bin/cli.js`'s dispatch switch (`handleExportPortfolio()`) plus a `COMMANDS['export-portfolio']` entry (and a hidden `COMMANDS.export` alias entry) in `src/help.js`.
- `handleExportPortfolio()` supports `-o`/`--out <path>` (default `./tech-overview.json`, parent directories created via `fs.mkdirSync(..., { recursive: true })`) and `--stdout` (prints the JSON payload and skips the file write entirely, rather than doing both).
- **Payload shape:** `{ generatedAt, overview, architecture, stack, roadmap }`. `overview` pulls `name`/`description` from `package.json` (falls back to dirname/empty string) plus `totalCompletedUows`/`latestMilestone` parsed from `.hydrate/PROJECT_JOURNAL.md`'s real completion-line convention (`- [x] **[<UOW-ID>]** <title> — <date> | Pass: <n>/<n> tests`, the same format `hydrate check`'s `hasProjectJournalEntry()` already asserts). `architecture` is an array of `{ id, title, date, body }` derived from `.hydrate/ARCHITECT_JOURNAL.md`'s `### `-delimited decision blocks — reuses `context.js`'s `extractRecentArchitectEntries(text, Infinity)` for the block-splitting instead of re-implementing it (`slice(-Infinity)` degrades to the full array). `stack` is extracted straight from `package.json` (name/version/license/runtime via `type`/engines/bin/dependency taxonomy) — the spec's "and DEV_JOURNAL.md annotations" was skipped since `DEV_JOURNAL.md` has no established stack-annotation convention to parse (a hand-maintained prose decision log, not structured data); `package.json` alone is a complete, well-defined source. `roadmap` is `{ scheduled, backlog }`, parsed from `.hydrate/ROADMAP.md`'s `## Section 1`/`## Section 2` headings (reusing the same section-slicing convention as `context.js`'s `extractActiveEpics()`), each entry `{ id, title, status, description }` via `parseRoadmapLine()` — handles both this repo's checkbox convention (`- [ ] **UOW-ID:** ...`, status `scheduled`/`done`) and plain concept bullets (`- **UOW-ID:** Title (detail)`, status `backlog`), with a trailing em-dash or parenthetical treated as the `description`.
- Every section degrades independently to an empty/null fallback (no thrown errors) when its source file, `.hydrate/` directory, or `package.json` is entirely missing or malformed — consistent with `context.js`/`check.js`'s existing resilience precedent.
- Added `tech-overview.json` to `.gitignore` since it's a generated build artifact (mirrors the existing `.hydrate/session.json` entry).
- Added `test/export-portfolio.test.js` (30 tests): unit coverage of every pure helper (`extractCompletedUows()`, `buildOverview()`, `buildArchitecture()`, `buildStack()`, `parseRoadmapLine()`, `buildRoadmap()`, `buildPortfolio()` including missing-`.hydrate/`, missing/malformed-`package.json`, and full-payload cases) plus CLI-level coverage (`--out` default/custom/`-o` short form with nested-directory creation, `--stdout` skipping the file write, the `export` alias, resilience against a completely absent `.hydrate/`, and `--help`).
- Test suite: 112/112 passing (85 prior + 27 new). Coverage: `src/commands/export-portfolio.js` 100% line / 94.74% branch / 100% funcs — clears the 80% line+branch gate; `bin/cli.js` 95.12% line / 77.55% branch (new `handleExportPortfolio()` lines fully covered; uncovered lines are pre-existing, out-of-scope edge cases).

### UOW-HYDRATE-05 — completed 2026-09-01
**Implement Smart State Machine & Session Reconciler (`hydrate checkup`)**

- Added `src/commands/checkup.js` exporting a pure `runCheckup(cwd)` (returns `{ state, message, ... }`) and `formatCheckupReport(result)`, and wired `hydrate checkup` (alias `hydrate status`) into `bin/cli.js`'s dispatch switch (`handleCheckup()`) plus a `COMMANDS.checkup` entry (and a hidden `COMMANDS.status` alias entry) in `src/help.js` — continues the `src/commands/*.js` pure-logic-vs-CLI-I/O split established by `check.js`/`context.js`/`export-portfolio.js`.
- `runCheckup()` reconciles `.hydrate/CURRENT_UOW.md`, `.hydrate/ROADMAP.md`, `.hydrate/archive/`, and `git status --porcelain` into one of five states: `clean-slate` (`CURRENT_UOW.md` empty/reset — reports whether `.hydrate/ROADMAP.md` has any pending content beneath its heading), `in-progress-dirty` / `in-progress-clean` (an active UOW with unchecked tasks — modified-file count from `git status` plus remaining-task count), `unarchived-done` (every task checked off and `npm test` passes — "run `hydrate complete`"), and `broken-build` (every task checked off but `npm test` fails). `error` covers a missing `.hydrate/` directory.
- `npm test` is only invoked when every `- [ ]` task in `CURRENT_UOW.md` is already checked off (`unarchived-done`/`broken-build` branches) rather than on every call — the only two states that actually depend on the test result, keeping `hydrate checkup` cheap for the common in-progress case.
- **Reused heuristic:** the "no remaining `- [ ]` tasks" signal is the same one `handleComplete()`'s `findOpenTasks()` and `check.js`'s `findUnarchivedCompletedUow()` already encode; also checks the resolved UOW ID against `.hydrate/archive/`'s filename stems (same contract `check.js` uses) so an already-archived-but-not-yet-reset `CURRENT_UOW.md` doesn't get re-flagged as "complete but unarchived".
- Both `git status --porcelain` and `npm test` spawn failures (binary not found, not a git repo) degrade gracefully rather than throwing — `getGitStatus()` treats "not a repo"/spawn error identically as "no git signal available", and `runTestSuite()`'s unrunnable case is treated the same as a passing test run (can't disprove completion, so don't block on it).
- Exit code: `0` for every state except `error` and `broken-build` (both `1`) — `hydrate checkup` is meant to be run as a quick, mostly-informational reconciler, not a hard CI gate, except when it detects an actual regression (broken build) or a missing `.hydrate/` structure.
- Added `test/checkup.test.js` (14 tests) covering: missing `.hydrate/`, clean-slate with/without pending roadmap content, the `status` alias, dirty vs. clean working tree with remaining tasks, complete-but-unarchived (tests pass) vs. broken-build (tests fail), a missing/whitespace-only `CURRENT_UOW.md`, a UOW payload with no resolvable ID (generic-label fallback), an already-archived UOW not being re-flagged, and both `git`/`npm` spawn-failure branches (forced via a blanked `PATH` env).
- Test suite: 126/126 passing (112 prior + 14 new). Coverage: `src/commands/checkup.js` 100% line / 82.61% branch / 100% funcs — clears the 80% line+branch gate; `bin/cli.js` 95.26% line / 78.00% branch (new `handleCheckup()` lines fully covered; uncovered lines are pre-existing, out-of-scope edge cases).

### UOW-HYDRATE-06 — completed 2026-09-01
**Implement Clipboard Ingress Engine & Interactive Prompting (`hydrate ingest`)**

- Added `src/commands/ingest.js` exporting a pure `runIngest(cwd, options)` (async — the only interactive command so far) plus testable helpers `isValidUowPayload()`, `isEmptyUow()`, `extractUowId()`, `renderBanner()`. Wired `hydrate ingest` (alias `hydrate paste`) into `bin/cli.js`'s dispatch switch (`handleIngest()`) plus a `COMMANDS.ingest` entry (and hidden `COMMANDS.paste` alias) in `src/help.js`.
- Extended `src/clipboard.js` (not a new `src/utils/` module as the UOW payload suggested — this repo has no `utils/` directory, and `clipboard.js` already owns clipboard I/O) with `readFromClipboard()`/`pasteCandidatesForPlatform()`, mirroring `copyToClipboard()`/`candidatesForPlatform()`'s platform-dispatch and injectable-`spawn` shape exactly. Paste tools: `pbpaste` (darwin), `powershell -NoProfile -Command Get-Clipboard` (win32 — `clip` is copy-only there), `xclip -selection clipboard -o` then `xsel --clipboard --output` (linux).
- **Validation contract:** `isValidUowPayload()` requires both a `# UOW-...`/`## UOW-...`/`### UOW-...` id header AND at least one of `Goal & Context` / `Surgical Scope` / `Acceptance Criteria` (case-insensitive) — requiring both, not either, avoids false-positiving on a stray `# UOW-` mention in unrelated clipboard text. An invalid payload prints a diagnostic to stderr, exits `1`, and leaves `.hydrate/CURRENT_UOW.md` completely untouched (checked before any file I/O).
- **Interactive flow:** renders the `⚙️ HYDRATE ENGINE` double-line box banner, then branches on `isEmptyUow()` (same placeholder/blank check `checkup.js`'s `isPlaceholderUow()` already encodes): empty → `[1] Apply` / `[2] Chat/Exit`; active/uncompleted → `[1] Archive current and swap` / `[2] Overwrite` / `[3] Chat/Exit`. Any unrecognized answer in either menu falls through to the chat-exit path (never a hard error) and exits `0` — the payload's requirement that `[Chat]` always be an available, safe exit.
- **`--yes`/`-y` flag:** applies non-interactively with no prompt — archives any active UOW first (if not empty), then writes the clipboard payload straight to `.hydrate/CURRENT_UOW.md`. Chosen over "always overwrite" as the non-interactive default since it's the non-destructive option (existing work is preserved in `.hydrate/archive/` either way).
- Every I/O seam (`readClipboard`, `input`/`output` streams, `log`/`error`) is injectable on `runIngest()`, so the full interactive flow — both menus, all branches, the invalid-answer fallback, and the synthesized-ID archive-naming edge case (no resolvable `UOW-` token in the outgoing payload) — is exercised directly with fake streams (`stream.Readable.from([...])` for stdin) instead of needing a real pty.
- Added `test/ingest.test.js` (33 tests): pure-helper unit coverage, direct `runIngest()` coverage of every menu branch (both empty-state options, both active-state options, the unrecognized-answer fallback, both `--yes` sub-cases, the no-clipboard-tool error, the invalid-payload rejection, the synthesized-archive-ID edge case), plus CLI-level end-to-end coverage (`hydrate ingest --yes` and `hydrate paste -y` through a fake `pbpaste`/`xclip` binary on `PATH`, the no-clipboard-tool exit-1 path, an unexpected-`mkdirSync`-failure exit-1 path exercising `handleIngest()`'s `.catch()` branch, and `--help`). Also added 10 tests to `test/clipboard.test.js` for `readFromClipboard()`/`pasteCandidatesForPlatform()`, mirroring the existing `copyToClipboard()`/`candidatesForPlatform()` suite.
- Test suite: 161/161 passing (126 prior + 35 new: 25 in `ingest.test.js`'s runIngest/helper/CLI sections plus 10 in `clipboard.test.js`). Coverage: `src/commands/ingest.js` 100% line / 97.50% branch / 100% funcs; `src/clipboard.js` 100% line / 100% branch / 100% funcs — both clear the 80% gate; `bin/cli.js` 95.46% line / 79.25% branch (new `handleIngest()` lines fully covered, including the `.catch()` branch; uncovered lines are pre-existing, out-of-scope edge cases).

### UOW-HYDRATE-07 — Claude Code Boot Hooks & Slash Command Definitions (2026-09-01)
- **Fast-Start Boot Protocol**: added a new "## 0. Fast-Start Boot Protocol" section to `templates/CLAUDE.md.template` and this repo's own `CLAUDE.md`, instructing Claude Code to run `hydrate checkup` on session boot and declaring `.hydrate/CURRENT_UOW.md`, `.hydrate/ROADMAP.md`, and `.hydrate/ARCHITECT_JOURNAL.md` as canonical context sources (suppressing legacy `CONTEXT.md` lookups).
- **Slash commands**: added `templates/.claude/commands/hydrate-checkup.md.template`, `hydrate-ingest.md.template`, and `hydrate-context.md.template` (mirrored into this repo's `.claude/commands/`), wrapping `hydrate checkup`, `hydrate ingest --yes`, and `hydrate context --clip` respectively for zero-touch interaction.
- **`src/init.js`**: `scaffold()` now also provisions `.claude/commands/` with the 3 new slash command files (idempotently, matching the existing HYDRATE_ARTIFACTS pattern via a new `CLAUDE_COMMANDS` list); `runInit()`'s next-steps output now mentions the new commands.
- **Tests**: updated `test/init.test.js` scaffold-count assertions (6→9 full scaffold, 5→8 when CLAUDE.md pre-exists) and added a slash-command-overwrite-protection case; added `test/claude-hooks.test.js` covering slash command template rendering, `scaffold()` wiring into `.claude/commands/`, and the CLAUDE.md template's Fast-Start Boot Protocol content.
- **Suite**: 171/171 passing (was 161/161). Coverage: `src/init.js` 100%/100% line/branch; all-files 98.58%/91.32%.

### UOW-HYDRATE-08 — completed 2026-09-01
**Add `hydrate lfg` Easter Egg Alias for Zero-Touch Session Launch**

- **CLI wiring**: added a case-insensitive `isLfgCommand` pre-check in `bin/cli.js` (`command.toLowerCase() === 'lfg'`, matching `lfg`/`LFG`/`Lfg`) ahead of the main `switch (command)` dispatch, routing to a new `handleLfg(rest)` — the existing switch is exact-match on `command`, and normalizing `command` itself for every case would have silently made every other verb case-insensitive too, so the check is scoped to `lfg` alone rather than touching the switch's match semantics.
- **`handleLfg()`** runs `runCheckup(cwd)` / `formatCheckupReport(result)` (same call `handleCheckup()` makes) to print the state-reconciliation report, then calls `handleIngest(options, { lfg: true })` — i.e. `lfg` is a real composition of `checkup` + `ingest`, not a separate reimplementation of either.
- **`handleIngest()`** signature extended with a second `{ lfg = false }` options object (kept separate from the first `options` array of raw CLI flags to avoid conflating flag parsing with the internal lfg-mode marker), forwarded to `runIngest(cwd, { yes, lfg })`.
- **Banner easter egg**: `src/commands/ingest.js`'s `renderBanner()` now takes an optional `{ lfg = false }` and appends a `🚀 LFG! Reconciling session state and diving in...` line inside the existing `⚙️ HYDRATE ENGINE` box only when `lfg` is true — `runIngest()` forwards its own `lfg` option through, so the greeting only appears when the command was actually invoked via the `lfg` alias, never on plain `hydrate ingest`.
- **Help surface**: `COMMANDS.lfg` added to `src/help.js` as a `hidden: true` entry (mirrors `paste`/`status`/`export`'s alias pattern) — resolvable via `hydrate lfg --help` but intentionally absent from the visible global command table, keeping it a genuine "easter egg" rather than an advertised verb.
- **Slash command**: added `templates/.claude/commands/hydrate-lfg.md.template` (mirrored into `.claude/commands/hydrate-lfg.md`), wrapping `hydrate lfg --yes` — deliberately NOT added to `src/init.js`'s `CLAUDE_COMMANDS` auto-scaffold list (the 3 zero-touch commands provisioned by `hydrate init` stay `checkup`/`ingest`/`context`, per UOW-HYDRATE-07); the `/hydrate-lfg` file exists for manual/explicit opt-in, matching the UOW's surgical file scope which named only the two `.claude/commands/` locations, not `src/init.js`.
- **Tests**: extended `test/ingest.test.js` with `renderBanner({ lfg })` on/off coverage, a direct `runIngest({ lfg: true })` case, and 3 CLI-level end-to-end cases (`lfg`/`LFG`/`Lfg`) asserting both the checkup report and the LFG banner line appear in stdout. Added new `test/help.test.js` covering the `COMMANDS.lfg` entry shape, `hydrate lfg --help`, and that `lfg` stays out of the visible `hydrate --help` command table.
- **Suite**: 180/180 passing (was 171/171). Coverage: `src/commands/ingest.js` 100% line / 97.56% branch; `src/help.js` 98.98% line / 82.61% branch; `bin/cli.js` 95.61% line / 79.63% branch — all files 98.60%/91.37%, clearing the 80% gate.

### UOW-HYDRATE-09 — completed 2026-09-01
**Add Triad Workflow Guide (`hydrate help` / `/hydrate-help`)**

- **`src/help.js`**: `printGlobalHelp()` now renders a `⚙️  HYDRATE ENGINE — TRIAD WORKFLOW GUIDE` block (right after the existing "THE HYDRATE WORKFLOW (1-2-3)" section) spelling out the 6-step cross-triad boot sequence: launch Claude Code in yolo mode (`claude --dangerously-skip-permissions`) → `hydrate init` → `/hydrate-context` → paste into the AI Architect (Gemini) chat → copy the generated UOW spec to the OS clipboard → `/hydrate-lfg`. Since `hydrate help`, `hydrate --help`, and `hydrate -h` all already route through `printGlobalHelp()` in `bin/cli.js`, no CLI dispatch changes were needed to satisfy all three invocations.
- **Slash command**: added `templates/.claude/commands/hydrate-help.md.template` (mirrored into this repo's `.claude/commands/hydrate-help.md`), wrapping `hydrate help`.
- **`src/init.js`**: `CLAUDE_COMMANDS` now includes `hydrate-help.md`, so `hydrate init` provisions all 4 zero-touch slash commands (`checkup`/`ingest`/`context`/`help`); `runInit()`'s next-steps footer mentions `/hydrate-help`.
- **Test fix (pre-existing regression risk)**: `test/help.test.js`'s "does not list the lfg easter egg" assertion previously scanned the *entire* `--help` stdout for `/\blfg\b/i`. The new Triad Guide legitimately mentions `/hydrate-lfg` as boot-sequence step 6 (outside the COMMANDS table), so the assertion was narrowed to scan only the text between the `COMMANDS` and `GLOBAL FLAGS` headers — preserving its actual intent (lfg stays out of the visible command table) without forbidding the guide's necessary mention of the alias.
- **Tests**: updated `test/init.test.js` (`CLAUDE_COMMAND_FILES` list, scaffold-count assertions 9→10 and 8→9, new `hydrate-help.md` checklist-output assertion); added 2 new tests to `test/help.test.js` covering the Triad Guide banner content across `hydrate help`, `hydrate --help`, and `hydrate -h`.
- **Suite**: 182/182 passing (was 180/180). Coverage: `src/help.js` 99.03% line / 82.61% branch; `src/init.js` 100%/100%; all-files 98.62%/91.37% — clears the 80% gate.

### UOW-HYDRATE-10 — completed 2026-09-01
**Zero-Touch Auto-Provisioning & Unified `/hydrate` Interface**

- **Clipboard module relocated**: moved `src/clipboard.js` → `src/utils/clipboard.js` (content unchanged) to match the UOW's `src/utils/clipboard.js` spec path; updated the two require sites (`bin/cli.js`, `src/commands/ingest.js`) plus `test/clipboard.test.js`'s import. The module's existing `pbcopy`/`clip`/`xclip`+`xsel` platform dispatch and injectable-`spawn` shape already satisfied the "OS-agnostic, headless-safe" acceptance criterion from UOW-HYDRATE-06 — no behavioral change, path-only.
- **`bin/postinstall.js`** (new): reads `process.env.INIT_CWD` (npm's "directory `npm install` was invoked from", distinct from `process.cwd()` during install) and calls a new `scaffoldClaudeCommands(cwd)` helper extracted from `src/init.js`'s `scaffold()` — provisions only `.claude/commands/`, non-destructively, without touching `CLAUDE.md` or `.hydrate/`. Guards against self-scaffolding into this repo's own tree during local `npm install` by skipping when `INIT_CWD` resolves to the package's own root. Wrapped in try/catch (fails quietly, mirroring the old `bin/setup-global.js` pattern) so a permissions error never breaks `npm install`. `package.json`'s `"postinstall"` script now points here instead of `bin/setup-global.js` (that file is left in place, untouched, but no longer wired up — the UOW's affected-files list didn't call for its removal).
- **`src/init.js`**: extracted the `.claude/commands/` provisioning loop out of `scaffold()` into standalone `scaffoldClaudeCommands(cwd)` (exported), reused by both `scaffold()` and `bin/postinstall.js` — avoids duplicating the idempotent "skip if file exists" loop. `CLAUDE_COMMANDS` grew from 4 to 7 entries: added `hydrate.md`, `hydrate-uow.md`, `hydrate-artifacts.md`.
- **`src/commands/uow.js`** (new): pure `listUows(cwd)`, `getLastUow(cwd)`, `getUowById(cwd, id)`, `formatUowList(result)`. "Chronologically" for `.hydrate/archive/` entries is derived from each archived file's `mtimeMs` rather than filename parsing — archive filenames don't share one numbering scheme (`UOW-01.md` vs. `UOW-HYDRATE-08.md` vs. `UOW-HYDRATE-01-HOTFIX.md`), but mtime is exactly the moment `hydrate complete` wrote each file, which is the true chronological signal. `getUowById()` checks the active canvas first (case-insensitive), then falls back to the archive.
- **`src/commands/artifacts.js`** (new): pure `buildArtifactsReport(cwd)` prints a plain-English tree of `.hydrate/` (journal files with one-line descriptions, plus an `archive/` count+listing) and `.claude/commands/` (each slash command with a one-line description), followed by a static recommended `.gitignore` block (`.hydrate/backups/`, `.hydrate/session.json`, `tech-overview.json` — matching the entries this repo's own `.gitignore` already carries) and a note that everything else under both directories should stay committed.
- **CLI routing**: `bin/cli.js` gained `case 'uow'` → `handleUow(rest)` (subcommands `list` [default when no arg given], `last`, `<id>`) and `case 'artifacts'` → `handleArtifacts()`. Registered both as full `COMMANDS` entries in `src/help.js` (not hidden aliases — these are new first-class verbs) with usage/when-to-run/what-it-does/examples, matching every other command's help-surface convention.
- **Unified master `/hydrate`**: `templates/.claude/commands/hydrate.md.template` (new) redefines the master command as a 3-step sequence — run `hydrate checkup`; if state is `unarchived-done`, run `hydrate complete` to auto-archive; run `hydrate context --clip` to refresh the clipboard payload; summarize for the Product Owner. This is prompt-only (no new CLI verb), consistent with every other `templates/.claude/commands/*.md.template` file, which are thin instruction wrappers around existing `hydrate` CLI verbs. Also added `hydrate-uow.md.template` (`hydrate uow $ARGUMENTS`, defaulting to `list`) and `hydrate-artifacts.md.template` (`hydrate artifacts`). Mirrored all three into this repo's own `.claude/commands/` (dogfooding) — including overwriting the repo's pre-existing hand-written `.claude/commands/hydrate.md` (previously just `hydrate prompt --copy`, predating UOW-HYDRATE-07's template scaffold) so `/hydrate` in this session reflects the new unified behavior.
- **Untouched by design**: `architect-context.md` and `docs/ARCHITECTURE.md` (both untracked, pre-existing in the working tree before this UOW started) are unrelated Product Owner scratch/reference files, not part of this UOW's affected-files list — left as-is, not staged.
- **Tests**: added `test/uow.test.js` (23 tests: `listUows()`/`getLastUow()`/`getUowById()`/`formatUowList()` unit coverage plus `hydrate uow [list|last|<id>]` CLI integration), `test/artifacts.test.js` (7 tests: `buildArtifactsReport()` unit coverage plus `hydrate artifacts` CLI integration), `test/postinstall.test.js` (6 tests: INIT_CWD scaffolding, process.cwd() fallback, non-destructive overwrite protection, self-install skip guard, and the quiet-failure catch branch). Updated `test/init.test.js`'s `CLAUDE_COMMAND_FILES` list and scaffold-count assertions (10→13 full scaffold, 9→12 when `CLAUDE.md` pre-exists) for the 3 new `CLAUDE_COMMANDS` entries.
- **Suite**: 211/211 passing (was 182/182; 29 new). Coverage: `src/commands/uow.js` 100% line / 96.67% branch; `src/commands/artifacts.js` 100% line / 91.30% branch; `bin/postinstall.js` 100%/100%; `src/utils/clipboard.js` 100%/100% — all clear the 80% gate; all-files 98.83% line / 91.91% branch.

### UOW-HYDRATE-11 — completed 2026-09-01
**System Architecture Maintenance & Decision Logging Protocol**

- **Scaffold templates**: renamed the pre-existing (untracked) `templates/docs/ARCHITECTURE.md` / `ARCHITECTURE_JOURNAL.md` to the required `.template` suffix so `src/templates.js`'s `renderTemplate()` naming convention (every source file is `templates/<name>.template`) holds for `docs/` the same way it already does for `.hydrate/` and `.claude/commands/`.
- **`src/init.js`**: added a `DOCS_ARTIFACTS` list (`ARCHITECTURE.md`, `ARCHITECTURE_JOURNAL.md`) and a `docs/` scaffold block in `scaffold()`, mirroring the existing `HYDRATE_ARTIFACTS` idempotent-write loop — creates `docs/` only if missing, skips any file that already exists. `CLAUDE_COMMANDS` grew from 7 to 9 entries: `hydrate-arch-sync.md`, `hydrate-digest.md`.
- **New slash commands**: `templates/.claude/commands/hydrate-arch-sync.md.template` (instructs CC to inspect the workspace and regenerate `docs/ARCHITECTURE.md` under the strict two-section layout) and `hydrate-digest.md.template` (a fixed reminder string pointing the Product Owner at the "Generate Architecture Journal Digest" prompt — no CLI logic, matching the UOW's exact spec text). Mirrored both into this repo's own `.claude/commands/` via `node bin/cli.js init` (dogfooding, same pattern as prior UOWs).
- **`CLAUDE.md` / `templates/CLAUDE.md.template`**: Section 6 (Architectural Logging & Journal Protocol) and Section 7 (System Architecture Maintenance Protocol) were already present in the working tree from a prior session but carried leftover `[span_N](start_span)/(end_span)` citation-tool artifacts in Section 7's list items — stripped those, and added an explicit `/hydrate-digest` trigger rule to Section 7 (previously only `/hydrate-arch-sync` was documented) so both new commands' handling is spelled out per the UOW's "Update CLAUDE.md Instructions" criterion.
- **Seeded `docs/ARCHITECTURE.md`**: the pre-existing untracked copy used a narrative "Architecture & File Plan" layout, not the strict two-section (`## 1. Overall System Architecture` / `## 2. Technology Stack & Dependencies`) structure Section 7 itself mandates. Rewrote it to a Mermaid component diagram (verified against the actual `src/commands/*.js` / `bin/*.js` file layout, not the stale `architect-context.md` file-plan text) plus a tech-stack bullet list — no sequence diagrams or decision-log content, per the section's own exclusion rule.
- **Seeded `docs/ARCHITECTURE_JOURNAL.md`**: same citation-artifact cleanup as `CLAUDE.md`, plus collapsed a duplicated top-level heading (`# Architect Journal - hydrate` immediately followed by `# Architecture Journal — @nemzilla/hydrate`) into one.
- **Untouched by design**: `architect-context.md` (untracked, pre-existing scratch output resembling a `hydrate context` clipboard dump) is not part of this UOW's affected-files list and isn't referenced by any source file — left in place, not staged.
- **Tests**: extended `test/init.test.js` — `CLAUDE_COMMAND_FILES` (+2), new `DOCS_FILES` list, scaffold-count assertions (13→17 full scaffold, 12→16 when `CLAUDE.md` pre-exists), new `docs/ARCHITECTURE*.md` existence + section-heading assertions, and `runInit()` checklist-output assertions for the 2 new command files and 2 new docs files.
- **Suite**: 211/211 passing (no net new test count change — existing scaffold tests were extended in place rather than duplicated). Coverage: `src/init.js` 100%/100%; all-files 98.84% line / 91.94% branch — clears the 80% gate.

### UOW-HYDRATE-12 — completed 2026-09-01
**Enrich Context Compiler with System Architecture & Rule Artifacts**

- **`src/commands/context.js`**: `buildContext()` now reads `CLAUDE.md` (repo root) and `docs/ARCHITECTURE.md` and, when present, appends them verbatim under `## Project Guidelines (CLAUDE.md)` and `## Overall Architecture (docs/ARCHITECTURE.md)` sections at the end of the payload. Absent either file, its section is omitted entirely (not replaced with a fallback notice) — matches the UOW's "if present" phrasing, distinct from the existing `.hydrate/*` sections which always print a fallback message.
- **Architectural-context fallback chain**: when `.hydrate/ARCHITECT_JOURNAL.md` has no `### ` entries, `buildContext()` now falls back first to `extractArchiveTitles(cwd, depth)` (new — reads the tail `depth` filenames out of `.hydrate/archive/*.md`, sorted, and pulls each file's leading `# ` title line), then to `extractProjectJournalEntries(projectJournalText, depth)` (new — tail `depth` `- [x]` checklist lines out of `.hydrate/PROJECT_JOURNAL.md`), and only then to the pre-existing "No architectural journal entries found." notice. Archive titles are preferred over `PROJECT_JOURNAL.md` entries since they carry the fuller UOW title text; both are exported for direct unit testing alongside the existing `extractRecentArchitectEntries`/`extractActiveEpics` helpers.
- **No CLI or clipboard changes**: `bin/cli.js`'s `handleContext()` already just pipes `buildContext()`'s return value to stdout/clipboard unchanged, so the enriched payload (verified manually via `node bin/cli.js context` against this repo's own `CLAUDE.md`/`docs/ARCHITECTURE.md`) flows through with no truncation, satisfying the "maintain clipboard export behavior" criterion with zero changes to `bin/cli.js` or `src/utils/clipboard.js`.
- **Tests**: extended `test/context.test.js` (the UOW's spec path `test/commands/context.test.js` doesn't match this repo's actual flat `test/` layout, which has no `test/commands/` subdirectory — updated the real file instead). Added unit coverage for `extractArchiveTitles()` (missing-dir case, tail-`depth` slicing) and `extractProjectJournalEntries()` (null/empty, tail-`depth` slicing), plus `buildContext()` tests for: CLAUDE.md/ARCHITECTURE.md inclusion when present, omission when absent, archive-title fallback taking precedence over PROJECT_JOURNAL.md when both exist, and PROJECT_JOURNAL.md fallback when archive/ is also empty.
- **Suite**: 219/219 passing (was 211/211; 8 new). Coverage: `src/commands/context.js` 100% line / 96.61% branch — clears the 80% gate; all-files 98.87% line / 91.86% branch.

### UOW-HYDRATE-13 — completed 2026-09-12
**Align CLI Subcommands with Slash Command Templates**

- **Audit**: compared every visible verb in `src/help.js`'s `COMMANDS` table against `templates/.claude/commands/*.md.template`. Of the UOW's 8 named verbs (`checkup`, `ingest`, `context`, `complete`, `uow`, `artifacts`, `check`, `export-portfolio`), 5 already had a template; `complete`, `check`, and `export-portfolio` did not.
- **3 new templates** (`hydrate-complete.md.template`, `hydrate-check.md.template`, `hydrate-export-portfolio.md.template`): each follows the established thin-wrapper convention — frontmatter `description` + a Bash-tool instruction to run the matching `hydrate <verb>` and summarize its report for the Product Owner. No new CLI logic was added; `bin/cli.js`'s existing `handleComplete`/`handleCheck`/`handleExportPortfolio` and their `src/commands/*.js` backers were left untouched since their behavior already matched what the new templates describe.
- **`src/init.js`**: `CLAUDE_COMMANDS` grew from 9 to 12 entries (added the 3 new templates); renumbered the trailing scaffold-step comments (`7-15` → `7-18` slash commands, `16-17` → `19-20` docs artifacts) to stay accurate.
- **Deliberately out of scope**: `init`, `prompt`, `clip`, `help`, and the `status`/`export`/`paste`/`lfg` aliases were not given (or don't need) their own new templates — `init`/`prompt`/`clip` run outside a Claude Code session by design, `help`/`status`/`export`/`paste` are covered by their canonical verb's existing template, and `lfg` already had one from UOW-HYDRATE-08.
- **Tests**: `test/init.test.js` — extended `CLAUDE_COMMAND_FILES`, scaffold-count assertions (17→20 full scaffold, 16→19 when `CLAUDE.md` pre-exists), and `runInit()` checklist-output assertions for the 3 new files. `test/claude-hooks.test.js` — extended `SLASH_COMMANDS` with the 3 new template/CLI-verb pairs (covered by the existing frontmatter, CLI-verb-invocation, and no-placeholder tests) and its scaffold-provisioning test's explicit label assertions.
- **Suite**: 219/219 passing (no net new test count — existing parameterized tests were extended in place rather than duplicated). Coverage: `src/init.js` 100%/100%; all-files 98.88% line / 91.86% branch — clears the 80% gate.

### UOW-HYDRATE-14 — completed 2026-09-12
**Global Lifecycle Engine & Workspace Sync**

- **Scope audit before writing code**: Items 1.2 (`/hydrate` auto-clip) and most of 1.3 (clipboard-first "Andiamo") turned out to already be implemented — `templates/.claude/commands/hydrate.md.template` already runs `hydrate checkup` → auto-`hydrate complete` → `hydrate context --clip`, and `hydrate lfg --yes` (backed by the existing `src/commands/ingest.js`) already does checkup-then-clipboard-first-ingest-with-disk-fallback. Only this repo's own local `CLAUDE.md` Section 5.4 (excluded from the template by design, per UOW-HYDRATE-08) needed updating to route the freeform trigger words ("Andiamo", "LFG", etc.) through `hydrate lfg --yes` instead of reading `.hydrate/CURRENT_UOW.md` directly. Item 1.4 (zero-touch cleanup gate) was likewise already satisfied by the existing `hydrate complete` + manual journal-append protocol (CLAUDE.md Section 4/5.1) — no code change needed there either. This left Item 1.1 and all of Track 2 as the actual net-new surface.
- **`src/utils/managed-block.js`** (new): `renderManagedBlock()`/`applyManagedBlock()`/`stripManagedBlock()` — a pure, idempotent "delimited block" primitive (`<!-- BEGIN/END HYDRATE MANAGED BLOCK vX.Y.Z -->`). `applyManagedBlock()` creates-fresh / appends-if-no-markers / replaces-only-the-delimited-text-if-markers-exist, exactly per Item 1.1's three-way execution logic.
- **`src/init.js`**: `scaffold()`'s CLAUDE.md step now goes through `applyManagedBlock()` instead of a skip-if-exists check — this is a deliberate behavior change from every other scaffolded artifact (which stay strictly skip-if-exists). A brownfield CLAUDE.md now gets the Hydrate rules appended inside a delimited block rather than being left untouched forever; re-running `hydrate init` with the same version is still a true no-op (`applyManagedBlock` output is byte-identical, so no write occurs and the file is omitted from the returned `created` list). Also added the previously-templated-but-never-wired `hydrate-lfg.md` to `CLAUDE_COMMANDS` (12→13 entries) — a template existed on disk from UOW-HYDRATE-08 but `scaffold()`'s array never included it, so fresh `hydrate init` runs silently never got `/hydrate-lfg`; fixed while touching this exact list since `hydrate sync` (below) needed an authoritative source of truth for "every command template that should exist." `CLAUDE_COMMANDS`/`HYDRATE_ARTIFACTS`/`DOCS_ARTIFACTS` are now exported for reuse by `src/commands/sync.js`.
- **`src/utils/repo-scan.js`** (new): `findRepoRoots()` — downward-only recursive repo discovery (`.git` or `package.json`), skipping `node_modules`/`.git` internals and other dotdirs. Deliberately does **not** scan upward toward parent directories despite Item 2.1/2.3's "parent/child directories" phrasing — an unbounded upward walk from an arbitrary cwd is unsafe for a `--recursive` flag; "this directory and everything below it" is the safe, predictable interpretation and is what both `hydrate sync --recursive` and `hydrate eject --recursive` use.
- **`src/commands/sync.js`** (new): `syncRepo()` bootstraps (full `scaffold()`) any target missing `.hydrate/`, otherwise force-overwrites every `.claude/commands/hydrate-*.md` to the current template render (unlike `scaffoldClaudeCommands()`'s skip-if-exists, sync's entire point is enforcing parity) and refreshes only the CLAUDE.md managed block — `.hydrate/` journals/archive/active-canvas are never touched. `runSync()` adds `--recursive`/`--path`.
- **`src/commands/eject.js`** (new): `planEject()`/`applyEjectActions()` separate plan-building from execution so `--dry-run` previews exactly what `--force` (or the default `readline` y/N prompt, mirroring `src/commands/ingest.js`'s existing interactive pattern) would do. Removes `.hydrate/`, every `.claude/commands/hydrate*.md`, and strips (or fully removes, if it was the file's only content) the CLAUDE.md managed block. Supports `--recursive`/`--path` via the same `findRepoRoots()`.
- **`src/utils/version-check.js`** (new): `fetchLatestVersion()` (injectable `spawn`, 1.5s timeout, `npm show <pkg> version`) + `getLatestVersion()` (24h TTL cache at `~/.hydrate/version-cache.json`, falls back to a stale cache on a failed live fetch) + `isNewerVersion()` (numeric segment-wise compare) + `checkVersionDrift()`/`formatDriftBanner()`.
- **`src/commands/update.js`** (new): `runUpdate()` shells `npm install [-g] @nemzilla/hydrate` (injectable `spawn`) then immediately calls `runSync()` on the current repo. **Never invoked for real against this machine during development** — global npm installs are a real side effect outside this UOW's authority; verified entirely through injected fakes.
- **`src/commands/hook.js`** (new): **Deviates from the spec's literal "global git template / `core.hooksPath`" mechanism** — git has no `post-init` hook event (hooks only fire on commit/checkout activity), so a template-dir hook can never reliably auto-run on a bare `git init`. Implemented the spec's own fallback option instead: a `git` shell-function wrapper installed into `~/.zshrc`/`~/.bashrc` inside a `# >>> hydrate git hook >>> / <<< ... <<<` delimited block (same idempotent-block shape as `managed-block.js`, kept as a separate small implementation rather than generalizing the HTML-comment version — the marker syntax differs and premature sharing wasn't worth it for ~15 lines). `enable`/`disable`/`status` are fully unit-tested against an injected `rcPath`; **the real `~/.zshrc` on this machine was never touched.**
- **Version-drift banner wiring (`bin/cli.js`)**: `handleCheckup()` now calls `checkVersionDrift()` after printing the checkup report, but only when `shouldCheckForUpdates()` (`process.stdout.isTTY && !process.env.CI`) is true. This is the same convention npm's own update-notifier uses to stay silent in CI/piped contexts, and it has the load-bearing side effect of keeping every existing CLI-level `spawnSync`-based test (checkup and lfg alike — non-TTY stdout) fully offline and network-free, since real `npm show` calls inside `npm test` would be slow, non-hermetic, and unsafe to assume network access for.
- **`bin/cli.js` routing**: added `sync`/`update`/`eject`/`hook` cases plus their handlers, and a shared `findFlagValue()` helper for `--path <dir>` / `--path=<dir>`. `handleUpdate`/`handleHook`'s CLI-argument-parsing lines are intentionally left uncovered by CLI-level spawn tests (only their `--help` paths are exercised) for the same safety reason as above — a real end-to-end invocation would run a global npm install or touch a real shell rc file; the actual business logic (`runUpdate`/`runHook`) already has 100% line coverage via injected-dependency unit tests.
- **`src/help.js`**: added full `COMMANDS` entries for `sync`/`update`/`eject`/`hook` (usage/when-to-run/what-it-does/options/examples, matching every other command's help surface) plus four new global-help example lines.
- **Tests**: `test/managed-block.test.js` (10), `test/repo-scan.test.js` (7), `test/version-check.test.js` (17), `test/sync.test.js` (8), `test/update.test.js` (4), `test/eject.test.js` (13), `test/hook.test.js` (9), `test/lifecycle-cli.test.js` (11 CLI-wiring tests, `--help`/dry-run/force/interactive-decline only — no real npm/git-config/shell-rc side effects). Updated `test/init.test.js` for the CLAUDE.md managed-block behavior change (scaffold counts 20→21, new "idempotent re-run doesn't duplicate the block" test, rewrote the two tests that previously asserted CLAUDE.md was skip-if-exists) and the `hydrate-lfg.md` addition to `CLAUDE_COMMAND_FILES`.
- **Suite**: 297/297 passing (was 219/219; 78 new). Coverage: every new module ≥90% line, ≥85% branch (`managed-block.js`/`repo-scan.js`/`pkg.js` 100%/100%, `sync.js` 100%/90%, `eject.js` 100%/97.44%, `hook.js` 100%/96.55%, `update.js` 100%/100%, `version-check.js` 98.06%/94.59%); all-files 98.48% line / 92.24% branch — clears the 80% gate.
